import React from 'react';
import Routes from './routes';

function App() {
  return <Routes />;
}

export default App;

// abrir a camera, tirar a foto, enviar por email (app padrão), nativo ou base64.
// Ler código de barra ou qr code e enviar código para um input.
// ler coordenadas do usuário(gps - latitude, longitude), abrir o maps e mostrar mapa com coordenadas.
// *opcional, garante os pontos na prova de 10 pontos*:
// desenhar mapa mostrando a distância da localização até o unipam.
